import { TitleFromRouteDataPipe } from './title-from-route-data.pipe';

describe('TitleFromRouteDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFromRouteDataPipe();
    expect(pipe).toBeTruthy();
  });
});
