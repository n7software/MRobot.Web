import { RouteIsSelectedPipe } from "./route-is-selected.pipe"

describe("RouteIsSelectedPipe", () => {
  it("create an instance", () => {
    const pipe = new RouteIsSelectedPipe()
    expect(pipe).toBeTruthy()
  })
})
